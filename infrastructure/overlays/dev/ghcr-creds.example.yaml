apiVersion: v1
kind: Secret
metadata:
  name: ghcr-creds
  namespace: ars
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: BASE64_ENCODED_DOCKERCONFIGJSON_EXAMPLE  # example only, do not use real secrets in git
